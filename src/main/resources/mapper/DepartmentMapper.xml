<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.contacts.mapper.DepartmentMapper">

    <!-- 插入部门 -->
    <insert id="insert" parameterType="com.contacts.pojo.Department">
        INSERT INTO department (name, supervisor_id)
        VALUES (#{name}, #{supervisor_id})
    </insert>

</mapper>